# deploy/.env.example — 部署配置模板
# 复制为 .env 并填入真实值：cp deploy/.env.example deploy/.env
# deploy/.env 已在 .gitignore 中，不会被提交

SERVER=root@YOUR_SERVER_IP
REMOTE=/opt/xzs-deploy
MYSQL_USER=root
MYSQL_PWD=YOUR_MYSQL_PASSWORD
MYSQL_DB=xzs
