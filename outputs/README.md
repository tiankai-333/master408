# outputs

临时输出目录。

用于放置本地生成的预览、渲染结果、调试中间文件等。正式交付材料应移动到 `docs/` 或对应业务目录。

## 典型内容

- DOCX/PPTX 渲染预览。
- 临时截图、contact sheet。
- 一次性脚本输出。

## 维护规则

1. 这里的内容默认可删除。
2. 不要把正式源文件只保存在这里。
3. 提交代码前检查是否误放了大文件或敏感文件。
